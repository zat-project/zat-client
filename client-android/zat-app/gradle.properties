# Mirrors the Telegram/library toolchain so the standalone + Telegram-hosted builds stay identical.
org.gradle.jvmargs=-Xmx4096M
# REQUIRED: the app + library use AndroidX; AGP fails resolution without this.
android.useAndroidX=true
android.enableJetifier=true
android.nonTransitiveRClass=false
